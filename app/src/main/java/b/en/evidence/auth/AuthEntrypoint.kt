package b.en.evidence.auth

import android.content.Context
import androidx.appcompat.app.AppCompatActivity

class AuthEntrypoint {
    fun doesAuthExist() : Boolean {

        return true
    }
    fun setAuth( ps: String ) : Boolean {
        return true
    }
}